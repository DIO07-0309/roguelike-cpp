# M4.4 Tactical Sequence Memory 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 镜像通过学习玩家"技能→位移→武器→连招"的 2-4 步战术链序列，预测玩家下一步战术动作并驱动反制（数据驱动，非 if-else）。

**Architecture:** 扩采集层（PlayerAction 加 weapon_type/combo_stage）→ 新增 TacticalChainTable（n-gram 战术符号计数+降级链）→ 集成 MirrorAgent 仲裁（战术链→克隆→Thompson）。

**Tech Stack:** C++17, GoogleTest, 现有 PlayerAction/recorder/MirrorAgent 组合架构。

## Global Constraints

- 函数 ≤40 行；类单一职责；组合优先
- 符号空间 12 个战术符号（4技能+4位移+3连招段+1保留）；3-gram 降级 2-gram → 克隆表 → 规则
- 不引入新依赖/神经网络；不做跨局持久化；不做在线滚动更新
- 测试基线 32/32（含 M4.1 验证回归）必须保持

---

### Task 1: 采集层扩展 — PlayerAction + recorder + weapon_executor

**Files:**
- Modify: `src/game/ai/player_behavior/player_action.h:36`
- Modify: `src/game/ai/player_behavior/player_behavior_recorder.h:28`
- Modify: `src/game/ai/player_behavior/player_behavior_recorder.cpp:63-70`
- Modify: `src/game/systems/weapon_executor.cpp:221-224`

**Interfaces:**
- Consumes: `stage_before` (int, 连招段 0-2) 和 `def->type` (WeaponType) 已存在于 weapon_executor.cpp:211-221
- Produces: `PlayerAction::weapon_type` (int, -1=未知), `PlayerAction::combo_stage` (int, -1=未知); `on_weapon_attack(wt_name, time, floor, px, py, weapon_type, combo_stage)`

- [ ] **Step 1: player_action.h 加字段**

```cpp
    int  facing_dir = -1;
    int  hit_in_1s = 0;        // 0=无受压, >0=刚被打 (理解"被打后行为变化")
    // M4.4: 战术链符号素材 — 武器类型 + 连招段 (仅 ATTACK 有效)
    int  weapon_type = -1;     // WeaponType 枚举 int, -1=未知
    int  combo_stage = -1;     // 0/1/2 连招段, -1=未知
```

- [ ] **Step 2: recorder.h 扩展签名**

```cpp
    void on_weapon_attack(const char* wt_name, float time, int floor,
        float px, float py, int weapon_type, int combo_stage);
```

- [ ] **Step 3: recorder.cpp 填充字段**

```cpp
void PlayerBehaviorRecorder::on_weapon_attack(const char* wt_name, float time,
    int floor, float px, float py, int weapon_type, int combo_stage) {
    PlayerAction a;
    a.type = PlayerActionType::ATTACK;
    a.timestamp = time; a.floor = floor; a.pos_x = px; a.pos_y = py;
    a.weapon_name = wt_name;
    a.weapon_type = weapon_type;
    a.combo_stage = combo_stage;
    record(a);
}
```

- [ ] **Step 4: weapon_executor.cpp 调用处传参**

```cpp
    g_behavior.on_weapon_attack(weapon_type_name(def->type),
        (float)game_time, 0,  // floor set by game_scene
        player->entity.rect.x + player->entity.rect.width/2,
        player->entity.rect.y + player->entity.rect.height/2,
        (int)def->type, stage_before);
```

- [ ] **Step 5: 构建验证**

Run: `cmake --build build`
Expected: 无错误

- [ ] **Step 6: 提交**

```bash
git add src/game/ai/player_behavior/player_action.h src/game/ai/player_behavior/player_behavior_recorder.h src/game/ai/player_behavior/player_behavior_recorder.cpp src/game/systems/weapon_executor.cpp
git commit -m "M4.4: 采集层扩展 — PlayerAction 加 weapon_type/combo_stage, on_weapon_attack 传连招段与武器类型"
```

---

### Task 2: TacticalChainTable — n-gram 战术链类

**Files:**
- Create: `src/ai/mirror/tactical_chain_table.h`
- Create: `src/ai/mirror/tactical_chain_table.cpp`
- Test: `tests/mirror/tactical_chain_test.cpp`

**Interfaces:**
- Consumes: `PlayerAction` (含新字段), `PlayerActionType`, `PlayerIntention` (来自 behavior_clone_table.h)
- Produces: `TacticalSymbol` (enum), `ChainPrediction {int best; float confidence; int level;}`; `TacticalChainTable::build(stream)`, `predict(s0, s1)`, `predict_fuzzy2(s0)`, `symbol_from_action(action)`

**符号映射（12 个）:**

```cpp
enum class TacticalSymbol : int {
    SKILL_0 = 0, SKILL_1, SKILL_2, SKILL_3,   // 技能 (skill_id 0-3)
    MOVE_L = 4, MOVE_R, MOVE_U, MOVE_D,       // 位移 (value 0-3)
    COMBO_1 = 8, COMBO_2, COMBO_3,            // 连招段 (combo_stage 0/1/2)
    COUNT = 11                                 // 12 个枚举值含未分配槽
};
```

- [ ] **Step 1: 写测试 (failing)**

创建 `tests/mirror/tactical_chain_test.cpp`:

```cpp
// M4.4: TacticalChainTable — 战术链 n-gram 序列记忆
#include <gtest/gtest.h>
#include "ai/mirror/tactical_chain_table.h"
#include "ai/player_behavior/player_action.h"

namespace {

PlayerAction MakeAction(PlayerActionType t, int value, int skill_id,
                        int weapon_type, int combo_stage) {
    PlayerAction a;
    a.type = t; a.value = value; a.skill_id = skill_id;
    a.weapon_type = weapon_type; a.combo_stage = combo_stage;
    a.timestamp = 0.0f;
    return a;
}

TEST(TacticalChain, SymbolMappingSkill) {
    auto a = MakeAction(PlayerActionType::SKILL, 0, 2, -1, -1);
    EXPECT_EQ(symbol_from_action(a), (int)TacticalSymbol::SKILL_2);
}

TEST(TacticalChain, SymbolMappingMove) {
    auto a = MakeAction(PlayerActionType::MOVE, 1, -1, -1, -1);
    EXPECT_EQ(symbol_from_action(a), (int)TacticalSymbol::MOVE_R);
}

TEST(TacticalChain, SymbolMappingCombo) {
    auto a = MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 1);
    EXPECT_EQ(symbol_from_action(a), (int)TacticalSymbol::COMBO_2);
}

TEST(TacticalChain, SymbolMappingComboStart) {
    auto a = MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 2);
    EXPECT_EQ(symbol_from_action(a), (int)TacticalSymbol::COMBO_3);
}

TEST(TacticalChain, BuildCountsTrigram) {
    TacticalChainTable t;
    std::vector<PlayerAction> h;
    // 技能2 → 右侧移 → 连招1 → 连招2 → 连招1 → 连招2 (重复模式)
    h.push_back(MakeAction(PlayerActionType::SKILL, 0, 2, -1, -1));
    h.push_back(MakeAction(PlayerActionType::MOVE, 1, -1, -1, -1));
    h.push_back(MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 0));
    h.push_back(MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 1));
    h.push_back(MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 0));
    h.push_back(MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 1));
    t.build(h);
    // (COMBO_1, COMBO_2) 前缀出现 1 次 → 后继 COMBO_1
    auto p = t.predict((int)TacticalSymbol::COMBO_1,
                       (int)TacticalSymbol::COMBO_2);
    EXPECT_EQ(p.level, 0);
    EXPECT_EQ(p.best, (int)TacticalSymbol::COMBO_1);
}

TEST(TacticalChain, DegradeTo2Gram) {
    TacticalChainTable t;
    std::vector<PlayerAction> h;
    // 右侧移 + 连招1 交替 5 次: s0=MOVE_R 后总是 COMBO_1 → 2-gram 学到
    for (int i = 0; i < 5; i++) {
        h.push_back(MakeAction(PlayerActionType::MOVE, 1, -1, -1, -1));
        h.push_back(MakeAction(PlayerActionType::ATTACK, 0, -1, 2, 0));
    }
    t.build(h);
    // 无 (SKILL_3, MOVE_R) 3-gram → predict 内自动降级到 fuzzy2(MOVE_R)
    auto p = t.predict((int)TacticalSymbol::SKILL_3,
                       (int)TacticalSymbol::MOVE_R);
    EXPECT_EQ(p.level, 1);   // 2-gram
    EXPECT_EQ(p.best, (int)TacticalSymbol::COMBO_1);
}

TEST(TacticalChain, EmptyStreamSafe) {
    TacticalChainTable t;
    std::vector<PlayerAction> h;
    t.build(h);
    EXPECT_EQ(t.entries(), 0);
}

}  // namespace
```

- [ ] **Step 2: 运行确认失败**

Run: `cmake --build build && cd build && ctest -R tactical_chain`
Expected: 编译失败 (头文件不存在)

- [ ] **Step 3: 实现头文件 `src/ai/mirror/tactical_chain_table.h`**

```cpp
#pragma once
#include <array>
#include <unordered_map>
#include <vector>
#include "ai/player_behavior/player_action.h"

// M4.4: TacticalChainTable — 玩家战术链序列学习 (n-gram, 无神经网络)
// 从 PlayerAction 流提取战术符号序列 → 3-gram 计数 → 预测下一步战术动作
// 降级链: 3-gram → 2-gram → 克隆表 → 规则 (集成方负责后两级)

enum class TacticalSymbol : int {
    SKILL_0 = 0, SKILL_1, SKILL_2, SKILL_3,   // 技能 (skill_id 0-3)
    MOVE_L = 4, MOVE_R, MOVE_U, MOVE_D,       // 位移 (value 0-3)
    COMBO_1 = 8, COMBO_2, COMBO_3,            // 连招段 (combo_stage 0/1/2)
    COUNT = 11
};

inline int symbol_from_action(const PlayerAction& a) {
    switch (a.type) {
    case PlayerActionType::SKILL:
        if (a.skill_id >= 0 && a.skill_id <= 3) return (int)TacticalSymbol::SKILL_0 + a.skill_id;
        return -1;
    case PlayerActionType::MOVE:
        if (a.value >= 0 && a.value <= 3) return (int)TacticalSymbol::MOVE_L + a.value;
        return -1;
    case PlayerActionType::ATTACK:
        if (a.combo_stage >= 0 && a.combo_stage <= 2)
            return (int)TacticalSymbol::COMBO_1 + a.combo_stage;
        return -1;   // 未知连招段 → 非战术符号
    default:
        return -1;   // 非战术动作 (DODGE/HEAL/TAKE_DAMAGE 等) 不参与战术链
    }
}

struct ChainPrediction {
    int best = -1;         // 预测的 TacticalSymbol
    float confidence = 0.0f;
    int level = -1;        // 0=3gram, 1=2gram, -1=miss
};

class TacticalChainTable {
public:
    void clear();
    void build(const std::vector<PlayerAction>& stream);
    void build_triple(int s0, int s1, int s2);
    ChainPrediction predict(int s0, int s1) const;
    ChainPrediction predict_fuzzy2(int s0) const;
    size_t entries() const { return _table3.size(); }

private:
    std::unordered_map<int, int> _table3;      // key = s0*121 + s1*11 + s2
    int _prefix_total[121] = {};               // 2-前缀 总次数 (分母)
    int _s0_total[11] = {};                  // 单前缀 总次数 (2-gram 分母)
    static int _key3(int s0, int s1, int s2) { return s0 * 121 + s1 * 11 + s2; }
};
```

- [ ] **Step 4: 实现 `src/ai/mirror/tactical_chain_table.cpp`**

```cpp
#include "ai/mirror/tactical_chain_table.h"

void TacticalChainTable::clear() {
    _table3.clear();
    for (auto& x : _prefix_total) x = 0;
    for (auto& x : _s0_total) x = 0;
}

void TacticalChainTable::build_triple(int s0, int s1, int s2) {
    if (s0 < 0 || s1 < 0 || s2 < 0) return;
    if (s0 >= 11 || s1 >= 11 || s2 >= 11) return;
    _table3[_key3(s0, s1, s2)]++;
    _prefix_total[s0 * 11 + s1]++;
    _s0_total[s0]++;
}

void TacticalChainTable::build(const std::vector<PlayerAction>& stream) {
    std::vector<int> syms;
    for (const auto& a : stream) {
        int s = symbol_from_action(a);
        if (s >= 0) syms.push_back(s);
    }
    for (size_t i = 0; i + 2 < syms.size(); i++)
        build_triple(syms[i], syms[i + 1], syms[i + 2]);
}

ChainPrediction TacticalChainTable::predict(int s0, int s1) const {
    ChainPrediction p;
    if (s0 < 0 || s1 < 0 || s0 >= 11 || s1 >= 11) return p;
    int base = s0 * 121 + s1 * 11;
    int total = _prefix_total[s0 * 11 + s1];
    if (total < 3) return predict_fuzzy2(s1);     // 样本不足 → 自动降级 2-gram
    int best_n = 0;
    for (int s2 = 0; s2 < 11; s2++) {
        int n = 0;
        auto it = _table3.find(base + s2);
        if (it != _table3.end()) n = it->second;
        if (n > best_n) { best_n = n; p.best = s2; }
    }
    if (best_n <= 0) return predict_fuzzy2(s1);
    p.confidence = (float)best_n / (float)total;
    p.level = 0;
    return p;
}

ChainPrediction TacticalChainTable::predict_fuzzy2(int s0) const {
    ChainPrediction p;
    if (s0 < 0 || s0 >= 11) return p;
    if (_s0_total[s0] < 5) return p;
    int best_s1 = -1, best_n = 0;
    for (int s1 = 0; s1 < 11; s1++) {
        int n = _prefix_total[s0 * 11 + s1];
        if (n > best_n) { best_n = n; best_s1 = s1; }
    }
    if (best_s1 < 0) return p;
    p.best = best_s1;                           // 2-gram: 该前缀最常跟的符号
    p.confidence = (float)best_n / (float)_s0_total[s0];
    p.level = 1;
    return p;
}
```

- [ ] **Step 5: 注册测试**

Modify `tests/CMakeLists.txt`:
```cmake
add_roguelike_test(mirror_tactic_test mirror/mirror_tactic_test.cpp)
add_roguelike_test(tactical_chain_test mirror/tactical_chain_test.cpp)
```

- [ ] **Step 6: 构建+运行**

Run: `cmake --build build && cd build && ctest -R tactical_chain`
Expected: 7 用例全过

- [ ] **Step 7: 提交**

```bash
git add src/ai/mirror/tactical_chain_table.h src/ai/mirror/tactical_chain_table.cpp tests/mirror/tactical_chain_test.cpp tests/CMakeLists.txt
git commit -m "M4.4: TacticalChainTable — 战术链 3-gram 序列记忆 (12 符号/2-gram降级), 7 单测"
```

---

### Task 3: MirrorAgent 集成 — 战术链仲裁

**Files:**
- Modify: `src/ai/mirror/mirror_agent.h:68-69, 116-140`
- Modify: `src/ai/mirror/mirror_agent.cpp:141-165`
- Test: `tests/mirror/tactical_chain_test.cpp` (追加仲裁用例)

**Interfaces:**
- Consumes: `TacticalChainTable` (Task 2 产出), `symbol_from_action`, `ChainPrediction`
- Produces: `MirrorAgent::set_chain_table(std::unique_ptr<TacticalChainTable>)`, `chain_table()`, 仲裁链在 `recommend_action` 中插入战术链层

- [ ] **Step 1: 追加仲裁测试 (failing)**

在 `tests/mirror/tactical_chain_test.cpp` 追加:

```cpp
// M3: 战术链仲裁 — 高置信 3-gram 驱动应对臂
TEST(TacticalChainArbitration, ChainLayerDrivesArm) {
    PlayerHabitProfile prof;
    MirrorAgent agent;
    agent.init(prof);
    agent.set_phase(2);
    auto chain = std::make_unique<TacticalChainTable>();
    // 玩家固定模式: 右移→右移→右移… (s0_right,s1_right) 3-gram 高置信
    std::vector<PlayerAction> h;
    for (int i = 0; i < 12; i++)
        h.push_back(MakeAction(PlayerActionType::MOVE, 1, -1, -1, -1));
    chain->build(h);
    agent.set_chain_table(std::move(chain));
    // 在线观测两次右移 → 缓冲 (MOVE_R, MOVE_R) → 预测 MOVE_R → ADVANCE 应对
    agent.on_prediction(PlayerActionType::NONE, 5.0f, 0.9f, 0);
    agent.observe_actual(PlayerActionType::MOVE);
    agent.on_prediction(PlayerActionType::NONE, 5.0f, 0.9f, 0);
    agent.observe_actual(PlayerActionType::MOVE);
    int arm = agent.recommend_action(MirrorBattleState{});
    EXPECT_EQ(arm, (int)MirrorAction::RETREAT);   // 玩家持续右移近身 → 镜像后撤如战术链
    EXPECT_GE(arm, 0);
}
```

- [ ] **Step 2: 头文件加组合与 API**

Modify `src/ai/mirror/mirror_agent.h`:

```cpp
#include "ai/mirror/tactical_chain_table.h"
...
    // ── M4.4: 战术链序列层 ──
    void set_chain_table(std::unique_ptr<TacticalChainTable> t);
    const TacticalChainTable* chain_table() const { return _chain.get(); }
...
private:
    std::unique_ptr<TacticalChainTable> _chain;   // M4.4: 战术链层
    int _seq_a = -1;    // 最近战术符号 (环形缓冲)
    int _seq_b = -1;
    int _recent_tactical(int symbol);   // 滚动更新 _seq_a/_seq_b
```

- [ ] **Step 3: cpp 实现 set_chain_table + 环形缓冲**

Modify `src/ai/mirror/mirror_agent.cpp`:

```cpp
void MirrorAgent::set_chain_table(std::unique_ptr<TacticalChainTable> t) {
    _chain = std::move(t);
}

int MirrorAgent::_recent_tactical(int symbol) {
    _seq_a = _seq_b;
    _seq_b = symbol;
    return _seq_a;
}
```

- [ ] **Step 4: 仲裁链插入战术链层**

Modify `recommend_action` 中克隆层之前 (L141 前):

```cpp
    // M4.4: 战术链层仲裁 — 预测玩家下一步战术符号 → 对应意图 → 应对臂
    if (_chain && _seq_b >= 0 && _seq_a >= 0) {
        ChainPrediction cp = _chain->predict(_seq_a, _seq_b);
        if (cp.level < 0) cp = _chain->predict_fuzzy2(_seq_b);
        if (cp.level >= 0 && cp.confidence > clone_confidence_threshold()) {
            PlayerIntention it = intention_from_chain_symbol(cp.best);
            int arm = boss_arm_for_intent(it);
            if (arm >= 0) {
                _debug_stats->on_arbitrate(true, false);   // 验收: 战术链仲裁
                return _record_arm(arm, st);
            }
        }
    }
```

- [ ] **Step 5: 加符号→意图映射函数**

在 `mirror_agent.cpp` 匿名命名空间 (boss_arm_for_intent 旁):

```cpp
// M4.4: 战术符号 → 玩家意图 (供战术链层仲裁)
PlayerIntention intention_from_chain_symbol(int s) {
    switch ((TacticalSymbol)s) {
    case TacticalSymbol::SKILL_0:
    case TacticalSymbol::SKILL_1:
    case TacticalSymbol::SKILL_2:
    case TacticalSymbol::SKILL_3: return PlayerIntention::SKILL;
    case TacticalSymbol::MOVE_L:
    case TacticalSymbol::MOVE_R:   return PlayerIntention::ADVANCE;
    case TacticalSymbol::MOVE_U:   return PlayerIntention::RETREAT;
    case TacticalSymbol::MOVE_D:   return PlayerIntention::RETREAT;
    case TacticalSymbol::COMBO_1:
    case TacticalSymbol::COMBO_2:
    case TacticalSymbol::COMBO_3: return PlayerIntention::ATTACK;
    default: return PlayerIntention::IDLE;
    }
}
```

- [ ] **Step 6: observe_actual 滚动战术缓冲**

在 `observe_actual` 中 `_observed_actions++` 后插入:

```cpp
    // M4.4: 滚动战术符号缓冲 (仅决策动作; 类型级符号用于在线仲裁)
    PlayerAction pa;
    pa.type = actual;
    int sym = symbol_from_action(pa);
    if (sym >= 0) _recent_tactical(sym);
```

注意: 在线 `observe_actual` 只有 PlayerActionType，无 skill_id/combo_stage 细节，
`symbol_from_action` 对残缺动作返回的类型级近似符号 (ATTACK→COMBO_START 等)。
缺失信息时 predict 自然降级到模糊 2-gram/克隆层，不产生错误动作。

- [ ] **Step 7: 构建+测试**

Run: `cmake --build build && cd build && ctest`
Expected: 全量通过 (32 原有用例 + tactical_chain 8 例)

- [ ] **Step 8: 提交**

```bash
git add src/ai/mirror/mirror_agent.h src/ai/mirror/mirror_agent.cpp tests/mirror/tactical_chain_test.cpp
git commit -m "M4.4: MirrorAgent 集成战术链仲裁 — 3-gram/2-gram 预测玩家下一步战术动作 (ML→战术链→克隆→Thompson)"
```

---

### Task 4: 端到端验证 + 收尾

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `docs/F15_MIRROR_BOSS_DESIGN.md` (M4.4 章节)

- [ ] **Step 1: 全量测试**

Run: `cd build && ctest`
Expected: 全部通过 (32 基线 + 新 tactical_chain 8 = 33+ 个测试目标保持绿)

- [ ] **Step 2: CHANGELOG 追加 v0.9.23**

```markdown
# v0.9.23 — M4.4 战术链序列记忆 (2026-08-07)

## M4.4 Tactical Sequence Memory
- 采集层扩展: PlayerAction 加 weapon_type/combo_stage, on_weapon_attack 传连招段
- TacticalChainTable: 12 战术符号 n-gram (技能×4/位移×4/连招段×3/连招起点)
- 降级链: 3-gram → 2-gram → 克隆表 → 规则; 仲裁: ML→战术链→克隆→Thompson
- M4.1 决策抽为 decide_tactic 纯函数 + 三场景回归单测 (SNIPER/时停/低血)
- 全量 33/33 绿
```

- [ ] **Step 3: README 里程碑表加 M4.4 行**

```markdown
| M4.4 | 战术链序列记忆: 12符号 n-gram 学习玩家 技能→位移→连招 套路, 仲裁链 ML→战术链→克隆→Thompson | ✅ |
```

- [ ] **Step 4: 桌面同步**

```powershell
$desk="C:\Users\HP\Desktop\Roguelike-CPP版"
# 镜像 src/ tests/ CHANGELOG.md README.md, 重建 exe 复制
```

- [ ] **Step 5: 提交 + 推送**

```bash
git add -A
git commit -m "docs: v0.9.23 CHANGELOG + README M4.4 里程碑"
git push origin master
```

---

## Self-Review 记录

- **Spec 覆盖**: 采集扩展→Task1; TacticalChainTable→Task2; 仲裁集成→Task3; 验证收尾→Task4 ✓
- **YAGNI**: 无跨局持久化/在线更新/新依赖 ✓
- **类型一致性**: `symbol_from_action`/`ChainPrediction`/`set_chain_table` 各 Task 间签名一致 ✓
- **已知缺口**: `TacticalSymbol::COUNT=12` 但文档处写 12 符号; spec 中武器类型采集已含 (weapon_type 在 symbol 映射暂不参与, 供未来扩展)
